$(function () {
    $(document).ready(function () {
        // Configurações padrão para todas as notificações
        // Configurações padrão para todas as notificações
        /*$.toastDefaults = {
            position: 'top-right', // Posição padrão da notificação
            bgColor: '#333', // Cor de fundo padrão
            textColor: '#fff' // Cor do texto padrão
        };

        // Configuração para notificações de erro
        $.toastError = {
            icon: 'error', // Ícone de erro
            bgColor: '#dc3545', // Cor de fundo para erro
            textColor: '#fff' // Cor do texto para erro
        };

        // Configuração para notificações de sucesso
        $.toastSuccess = {
            icon: 'success', // Ícone de sucesso
            bgColor: '#28a745', // Cor de fundo para sucesso
            textColor: '#fff' // Cor do texto para sucesso
        };*/
    });
})